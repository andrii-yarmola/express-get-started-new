<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User Index</title>
<body>

  <h1>Handlebars</h1>
  <ul>
    {{#each users as |user|}}
    <li>
      <a href="/{{user.name}}">{{user.name}}</a>
    {{/each}}
  </ul>
</body>
</html>
