<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User Index</title>
<body>

  <h1>Handlebars</h1>
  <ul>
    {{#each users as |user|}}
    <li>
      <a href="/user/{{user.name}}">{{user.name}}</a>
      <img src="/profilepics/{{user.image}}.jpg" alt="image">
    {{/each}}
  </ul>
</body>
</html>
